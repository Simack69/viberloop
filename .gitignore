# Firebase credentials - never commit the real config to git
# Use firebase-config.example.js as a template and deploy firebase-config.js separately
firebase-config.js

# Node modules
node_modules/

# Build artifacts
css/tailwind.css

# OS files
.DS_Store
Thumbs.db
